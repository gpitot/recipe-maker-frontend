import React, { useEffect } from "react";
//import API from "rest/api";

const Shop = () => {
  useEffect(() => {
    // API.shop.getTransactions().then((res) => {
    //   console.log("transactions ", res);
    // });
  }, []);

  return (
    <section>
      <h1>Coming soon</h1>
    </section>
  );
};

export default Shop;
